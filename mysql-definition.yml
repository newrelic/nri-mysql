name: com.newrelic.mysql
description: Reports status and metrics for mysql server
protocol_version: 2.1
os: linux

commands:
    status:
        command:
            - ./bin/nr-mysql
        prefix: config/mysql
        interval: 30
