FROM mysql:latest
# Create and set permissions for log directory
RUN mkdir -p /var/log/mysql && \
    chown -R mysql:mysql /var/log/mysql
COPY tests/integration/mysql-config-files/master.conf.cnf /etc/mysql/conf.d/mysql.conf.cnf
